#include "main.h"

/**
 * mul - function that returns multiplication of two integers
 *
 * @a: one of the integers
 * @b: one of the integers
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
